$schema: https://azuremlschemas.azureedge.net/latest/amlCompute.schema.json
name: batch-cluster
type: amlcompute
size: STANDARD_DS4_V2
min_instances: 0
max_instances: 10
tier: dedicated
# Note: GitHub Actions workflow creates compute dynamically via create-compute.yml
# If using this file directly with Azure ML CLI, configure identity as follows:
# identity:
#   type: user_assigned
#   user_assigned_identities:
#     - resource_id: /subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.ManagedIdentity/userAssignedIdentities/<identity-name>
